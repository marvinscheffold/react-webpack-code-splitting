import * as React from "react";

export function LoadingOverlay(){
    return(
        <div style={{width:"100vw", height: "100vh", color:"white", backgroundColor: "black"}}>
            <p>Loading...</p>
        </div>
    )
}